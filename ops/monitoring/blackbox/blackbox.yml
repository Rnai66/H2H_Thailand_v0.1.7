modules:
  http_2xx:
    prober: http
    timeout: 5s
    http:
      method: GET
      valid_http_versions: ["HTTP/1.1", "HTTP/2.0"]
      preferred_ip_protocol: "ip4"
      # ถ้าจะเช็ค header/keyword เพิ่มเติม ใส่ regex ใน fail_if_body_not_matches ได้
